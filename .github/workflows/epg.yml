name: Update Alpha EPG

on:
  schedule:
    # 21:02 UTC είναι 00:02 ώρα Κύπρου (για θερινή ώρα UTC+3)
    - cron: '2 21 * * *'
  workflow_dispatch: # Σου επιτρέπει να το τρέχεις χειροκίνητα για δοκιμή

jobs:
  update-epg:
    runs-on: ubuntu-latest
    # Προσθέτουμε permissions για να μπορεί το bot να κάνει push
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests beautifulsoup4

      - name: Run epg.py
        run: python epg.py

      - name: Commit and push epg.xml if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          
          git add epg.xml
          
          # Έλεγχος αν υπάρχουν αλλαγές πριν το commit
          if ! git diff --staged --quiet; then
            git commit -m "Update Alpha EPG (Current + Next Day)"
            git push
          else
            echo "No changes in epg.xml"
          fi
